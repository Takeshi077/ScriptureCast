services:
  - type: web
    name: scripturecast
    runtime: python
    buildCommand: ./start.sh
    startCommand: python run.py
    envVars:
      - key: ASSEMBLYAI_API_KEY
        sync: false
      - key: JWT_SECRET
        sync: false
